git init	//initialize current folder as git project
git add .	//add all items in the folder to staging area and ready to be the formal version
git commit	//commit items in the satging area as the formal version
	-m	//leave a 1-line message
	-e	//leave a complicated message with editer
git log		//check the commit log
git status	//check the current modified and stage status
git reset HEAD <filename>	//remove the file from staging area
git rm --cached <filename>	//remove the file from commiting
git diff	//check the modified file
git pull	//pull the items online

